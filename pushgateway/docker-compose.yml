version: '3.7'
services:
  pushgateway:
    image: prom/pushgateway
    volumes:
    - ./prometheus_data:/prometheus
    ports:
      - "9091:9091"
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.librenms.rule=Host(`pushgateway.monitor.com`)"
      - "traefik.http.routers.librenms.entrypoints=web"
      - "traefik.http.services.librenms.loadbalancer.server.port=80"
networks:
  default:
    external: true
    name: reverse-proxy